import '../style/style.scss'



/*
import IndexModel from './models/indexModel'
import ContactModel from './models/contactModel'

import View from './views/View'
import IndexView from './views/indexView'

const Controller = {};


Controller.Index = {};

Controller.Index.header = function() {
    const header = IndexView.header(IndexModel.data.iconAbout);
    View.DOM.templateContents.appendChild(header);

    return this;
}

Controller.Index.article = function() {
    const article = IndexView.article(IndexModel.data, ContactModel.data);
    View.DOM.templateContents.appendChild(article);

    return this;
}

Controller.Index.worksMenu = function() {
    const worksMenu = IndexView.worksMenu(IndexModel.data.worksMenu);
    View.DOM.templateContents.appendChild(worksMenu);

    return this;
}

Controller.Index.header().article().worksMenu();
*/
